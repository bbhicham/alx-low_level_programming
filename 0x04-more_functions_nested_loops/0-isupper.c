#include "main.h"

/**
 * _isupper- check if char is Uppercase
 * @c: is the char => to be checked
 * Return: 1 if charcter is Uppercase and 0 is otherwise.
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}

}
